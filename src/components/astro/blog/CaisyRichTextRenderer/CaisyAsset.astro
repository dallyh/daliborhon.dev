---
import { Image } from "astro:assets";

export interface Props {
    src: string;
    description: string;
    width: number;
    height: number;
}

const { src, description, width, height } = Astro.props;
---

<Image width={width} height={height} src={src} alt={description} placeholder="blurhash" />
