---
import { getEntry, render } from "astro:content";

const entry = await getEntry("resume", "cs");

if (!entry) {
    return new Response(null, { status: 500 });
}

const { Content } = await render(entry);
---

<Content />