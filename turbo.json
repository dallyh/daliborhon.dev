{
    "$schema": "https://turbo.build/schema.json",
    "envMode": "loose",
    "tasks": {
        "build": {
            "outputs": ["dist/**/*"],
            "dependsOn": ["^build"]
        }
    }
}
