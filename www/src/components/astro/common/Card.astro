---
interface Props {
	isArticle?: boolean;
}

const { isArticle = false } = Astro.props;
const Tag = isArticle ? "article" : "div";
---

<Tag class="card bg-base-200 card-md relative h-full max-w-full shadow-md">
	<slot />
</Tag>
