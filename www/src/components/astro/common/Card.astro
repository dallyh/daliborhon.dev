---
interface Props {
	isArticle?: boolean;
}

const { isArticle = false } = Astro.props;
const Tag = isArticle ? "article" : "div";
---

<Tag class="card bg-base-200/60 card-md relative h-full max-w-full shadow-md backdrop-blur-xs">
	<slot />
</Tag>
